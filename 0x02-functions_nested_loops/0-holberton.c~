#include "holberton.h"
/**
* _putchar - writes the character c to stdout
* Description: using  _putchar to print a string
*
*Return: On success 1.
*On error, -1 is returned, and errno is set appropriately.
*/
int main(void)
{
	char c = "Holberton";

	_putchar(c);
	return (0);
}
